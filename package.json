{
  "name": "auk-smart-parking-system",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "npm run build --prefix frontend && npm run seed --prefix backend && cross-env NODE_ENV=production npm run start --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm install --workspaces && npm run build --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "test": "npm run test --prefix backend"
    },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "cross-env": "^10.1.0"
  }
}
